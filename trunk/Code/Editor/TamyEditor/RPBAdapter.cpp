#include "RPBAdapter.h"
#include "core-Renderer/RPStartNode.h"


///////////////////////////////////////////////////////////////////////////////

BEGIN_OBJECT( RPBAdapter, RenderingPipelineBlock );
PROPERTY( TResourceHandle< RPAdapterNode >*, m_node );
END_OBJECT();

///////////////////////////////////////////////////////////////////////////////

RPBAdapter::RPBAdapter( RPAdapterNode& node )
   : RenderingPipelineBlock( node )
   , m_nodePtr( &node )
{
   setCaption( "Adapter" );
}

///////////////////////////////////////////////////////////////////////////////

RPBAdapter::~RPBAdapter()
{
   delete m_node;
   m_node = NULL;
}

///////////////////////////////////////////////////////////////////////////////

void RPBAdapter::initialize( RenderingPipeline& parentResource )
{
   m_node = new TResourceHandle< RPAdapterNode >( *m_nodePtr );
}

///////////////////////////////////////////////////////////////////////////////

Object& RPBAdapter::getNode() 
{ 
   return m_node->get();
}

///////////////////////////////////////////////////////////////////////////////
